name: trivy

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '17 23 * * 1'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  trivy-fs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Trivy filesystem scan (repo)
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          scan-type: 'fs'
          format: 'sarif'
          output: 'trivy-fs.sarif'
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          vuln-type: 'os,library'

      - name: Upload SARIF (FS)
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-fs.sarif

  trivy-images:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - { name: nginx,                 dockerfile: 'hive/nginx/Dockerfile',                     context: 'hive/nginx' }
          - { name: hive-elasticsearch,    dockerfile: 'hive/elasticsearch/Dockerfile',             context: 'hive/elasticsearch' }
          - { name: hive-kibana,           dockerfile: 'hive/kibana/Dockerfile',                    context: 'hive/kibana' }
          - { name: hive-logstash,         dockerfile: 'hive/logstash/Dockerfile',                  context: 'hive/logstash' }
          - { name: filebeat,              dockerfile: 'sensor/filebeat/Dockerfile',                context: 'sensor/filebeat' }
          - { name: madcat,                dockerfile: 'sensor/madcat/Dockerfile',                  context: 'sensor/madcat' }
          - { name: rl-agent,              dockerfile: 'hive/rl-agent/Dockerfile',                  context: 'hive/rl-agent' }
          - { name: rl-agent-filebeat,     dockerfile: 'hive/rl-agent-filebeat/Dockerfile',         context: 'hive/rl-agent-filebeat' }
          - { name: k8s-orchestrator,      dockerfile: 'hive/orchestrator/Dockerfile',              context: 'hive/orchestrator' }
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Build ${{ matrix.image.name }}
        run: |
          docker build \
            -t local/${{ matrix.image.name }}:${{ github.sha }} \
            -f ${{ matrix.image.dockerfile }} \
            ${{ matrix.image.context }}

      - name: Trivy scan ${{ matrix.image.name }}
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          image-ref: local/${{ matrix.image.name }}:${{ github.sha }}
          format: 'sarif'
          output: trivy-${{ matrix.image.name }}.sarif
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
